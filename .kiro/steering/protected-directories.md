---
inclusion: always
---

# Protected Directories

## kiwi-image-descriptions-examples/

The `kiwi-image-descriptions-examples/` directory is a Git submodule containing another repository.

**CRITICAL RULES:**
- DO NOT make any changes to files in `kiwi-image-descriptions-examples/`
- DO NOT add new files to `kiwi-image-descriptions-examples/`
- DO NOT delete files from `kiwi-image-descriptions-examples/`
- DO NOT modify any existing files in `kiwi-image-descriptions-examples/`
- This directory is read-only for reference purposes only

If the user requests changes that would affect this directory, politely explain that it's a separate repository and cannot be modified from this workspace.
